/** @type {import('next').NextConfig} */
// const nextConfig = {};

// module.exports = nextConfig;
module.exports = {
  images: {
    domains: ["localhost"], // Ajoutez le domaine de vos images ici (par exemple, 'mon-site.com')
  },
};
